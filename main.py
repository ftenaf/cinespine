# main.py
# This file is present to help Replit auto-detect the language of the project.
# The actual entrypoint is in backend/app/main.py.

import os
import sys

if __name__ == "__main__":
    print("Please run this project using the Replit 'Run' button, which executes the custom build/run commands in .replit")
    print("If you are running this locally, use 'uvicorn backend.app.main:app --reload'")
